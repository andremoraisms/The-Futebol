
var navItem = document.querySelectorAll('.nav-list')

function selecionarPag(){
    navItem.forEach((pag)=>
        item.classList.remove('ativo')
)
    this.classList.add('ativo')
}

navItem.forEach((pag)=>
    pag.addEventListener('click', selecionarPag)
)